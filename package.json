{
  "name": "vzome-web-import",
  "version": "0.0.1",
  "devDependencies": {
    "esbuild": "^0.13.7",
    "regenerator-runtime": "^0.13.9"
  },
  "dependencies": {
    "@vzome/react-vzome": "^0.9.24",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "build": "npx esbuild --format=esm --bundle --splitting --target=es2020 --outdir=dist/js src/js/**/*.js src/js/*/*/*.js",
    "clean": "rm-rf ./dist"
  }
}
